{
  "name": "navigwiz-auth-worker",
  "version": "1.0.0",
  "description": "Navigwiz Auth Worker — Cloudflare edge auth for navigwiz.acronous.com",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.117.0"
  }
}
